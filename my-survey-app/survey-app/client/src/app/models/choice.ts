export class Choice {
    // constructor(public id: number, public surveyName: string, public choice_text: string, public count: number){

    // }
    constructor( public text: string, public count: number){

    }
}